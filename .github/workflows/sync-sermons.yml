# name: Sync Sermon Archive

# on:
#   schedule:
#     # Daily at 13:00 UTC (~8-9am Eastern) — sermons post 1-2x/week, daily is plenty.
#     - cron: '0 13 * * *'
#   workflow_dispatch: {}

# permissions:
#   contents: write

# jobs:
#   sync:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Sync sermons from YouTube + apply any pending enrichments
#         env:
#           YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
#           YOUTUBE_CHANNEL_ID: ${{ secrets.YOUTUBE_CHANNEL_ID }}
#         run: node scripts/sync-sermons.mjs

#       - name: Commit changes if any
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git add src/content/sermons/sermons.json data/sermon-enrichments
#           if git diff --staged --quiet; then
#             echo "No sermon changes to commit."
#           else
#             git commit -m "Sync sermon archive from YouTube"
#             git push
#           fi
